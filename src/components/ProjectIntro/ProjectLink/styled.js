import styled from 'styled-components';
import { ButtonSecondary } from '../../shared/Button/variations/styled';

export const ProjectLinks = styled.div`
  display: flex;
`;

export const ProjectLink = styled(ButtonSecondary)`
  margin-top: 2.4rem;
  margin-left: -0.8rem;
`;
